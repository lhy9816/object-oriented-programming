package com.calvin.oohw3.enums;

public enum TokenType {
    // for stop searching and wrong answer
    QUIT, WA, INIT,
    // PRESERVING WORDS
    SIN, COS,
    // operators
    PLUSSY, SUBSY, MULTSY, POWSY, LSBRASY, RSBRASY,
    // elements
    SPACE, NUMB, XS
}
